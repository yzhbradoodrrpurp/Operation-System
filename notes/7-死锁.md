# 死锁

## 死锁基本概念

在并发环境下，各进程因竞争资源而造成的一种互相等待对方手里的资源，导致各进程都阻塞都无法向前推进的现象。

| 类型   | 区别                                                         |
| ------ | ------------------------------------------------------------ |
| 死锁   | 死锁一定是循环等待对方手里的资源导致的。如果有死锁，则至少有两个进程处于**阻塞态**。 |
| 饥饿   | 可能只有一个进程发生饥饿现象，发生饥饿的进程可能是阻塞态 (需要 IO 设备)，也可能是就绪态 (需要处理机)。 |
| 死循环 | 可能只有一个进程发生死循环，死循环的进程可以上处理机运行，只不过无法顺利推进。 |

**产生死锁的四个必要条件**：

- 互斥：只有对必须互斥使用的资源的争抢才会导致死锁
- 不剥夺：进程所获得的资源在未使用完成前，不能由其它进程强行夺走
- 请求和保持：该进程已经保持了至少一个资源，但是又提出了新的资源请求
- 循环等待：存在一种进程资源的循环等待链，链中每个进程所需要的资源被另一个进程占用

## 死锁的处理策略

### 预防死锁：破环死锁产生的四个必要条件

#### 破坏互斥条件

用 SPOOLing 技术来破坏互斥访问条件

![preventdeadlock](/Users/yzhbradoodrrpurp/Desktop/OS/notes/resources/preventdeadlock.png)

缺点：并不是所有的资源都可以改造成可共享使用的资源，并且为了系统安全，很多资源必须保护互斥性。

#### 破坏不剥夺条件

![preventdl](/Users/yzhbradoodrrpurp/Desktop/OS/notes/resources/preventdl.png)

#### 破坏请求和保持条件

可采用**静态分配法**来破坏请求和保持条件。

![prevent](/Users/yzhbradoodrrpurp/Desktop/OS/notes/resources/prevent.png)

#### 破坏循环等待条件

可采用**顺序资源分配法**来破坏循环等待条件。

![pv](/Users/yzhbradoodrrpurp/Desktop/OS/notes/resources/pv.png)

### 避免死锁

#### 安全序列

如果系统按照这种序列分配资源，则个进程都能顺利完成，这种序列就是安全序列。

> [!NOTE]
>
> 能找出一种安全序列的系统就是安全系统。

> [!NOTE]
>
> **如果系统处于安全状态，则一定不会发生死锁；如果系统进入不安全状态，则可能发生死锁**。

#### 银行家算法

<iframe width="560" height="315" src="https://www.youtube.com/embed/T0FXvTHcYi4?si=F94ubPjLe_BSQSCp" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" referrerpolicy="strict-origin-when-cross-origin" allowfullscreen></iframe>

> [!NOTE]
>
> 注意区别**死锁预防**和**死锁避免**：
>
> - 死锁预防：从根源上解决死锁问题，破坏死锁发生的四个必要条件
> - 死锁避免：允许有风险，但是实时判断绕开危险状态

### 死锁的检测和解除

#### 检测

数据结构图：

- 两种节点
  - 进程节点：对应一个进程
  - 资源节点：对应一种资源，同一种资源可能有多个
- 两种边
  - 进程节点 $\rightarrow$ 资源节点：进程想申请的资源
  - 资源节点 $\rightarrow$ 进程节点：资源已经被分配给的进程

![handledl](/Users/yzhbradoodrrpurp/Desktop/OS/notes/resources/handledl.png)

![detectdl](/Users/yzhbradoodrrpurp/Desktop/OS/notes/resources/detectdl.png)

#### 解除

- 资源剥夺法：挂起（暂时放到外存上）某些死锁进程，并抢占它的资源，将这些资源分配给其他的死锁进程
- 撤销进程法：强制撤销部分、甚至全部死锁进程，并剥夺这些进程的资源
- 进程回退法：让一个或者多个死锁进程回退到避免死锁的地步

