# 处理机调度

## 调度的基本概念

调度：当有一堆任务要处理，但是由于资源有限，这些任务没法同时处理。这就需要确定某种规则来决定处理这些任务的顺序。

![scheduling](/Users/yzhbradoodrrpurp/Desktop/OS/notes/resources/scheduling.png)

## 调度的三种层次

### 高级调度

高级调度：又称为作业调度，按一定原则从外存的作业后备队列中挑选一个作业进入内存并创建进程。

- 每个作业只调入一次，调出一次
- 作业调入时会创建 PCB，调出时会销毁 PCB

> [!NOTE]
>
> 作业：一个具体的任务。

### 低级调度

低级调度：又称为进程调度，按照某种策略从就绪队列中选取一个进程并将处理机分配给它。

> [!NOTE]
>
> 进程调度是操作系统中最基本的一种调度，在一般的操作系统中都必须配置进程调度。

### 中级调度

中级调度：又称为内存调度，内存不够时，可将某些进程的数据调出外存，等内存空闲或者进程需要运行时再重新调入内存。

### 三种调度的比较

![comparison](/Users/yzhbradoodrrpurp/Desktop/OS/notes/resources/comparison.png)

## 进程调度的方式

### 非剥夺调度方式

非剥夺调度方式：又称为非抢占方式，只允许进程主动放弃处理机。在运行过程中即便有更紧迫的任务到达，当前进程依然会继续使用处理机，直到该进程终止或主动要求进入阻塞态。

### 剥夺调度方式

剥夺调度方式：又称抢占方式，当一个进程正在处理机上执行时，如果有一个更重要的进程需要使用处理机，则立即暂停正在执行的进程，将处理机分配给更重要紧迫的进程。

## 调度算法的指标

### CPU 利用率

$CPU 利用率 = \frac{忙碌时间}{总时间}$

> [!TIP]
>
> 某计算机只支持单道程序，某个作业刚开始需要在CPU上运行5秒，再用打印机打印输出5秒，之后再执行5秒，才能结束。在此过程中，CPU利用率是多少？
>
> $CPU 利用率 = \frac{5+5}{5+5+5} = 66.66\%$

### 系统吞吐量

$系统吞吐量 = \frac{总共完成的作业数}{总共用时}$

> [!TIP]
>
> 某计算机系统处理完10道作业，共花费100秒，则系统吞吐量为？
>
> $系统吞吐量 = \frac{10}{100} = 0.1 道/秒$

### 周转时间

$周转时间=作业完成时间-作业提交时间$

$平均周转时间 = \frac{1}{n} \sum_{i=1}^n{周转时间_i}$

$带权周转时间=\frac{作业完成时间-作业提交时间}{作业实际运行时间}$

$平均带权周转时间 = \frac{1}{n} \sum_{i=1}^n{带权周转时间_i}$​​

### 等待时间

等待时间：进程/作业处于等待处理机状态时间之和。

### 响应时间

响应时间：用户提出请求到第一次响应所产生的时间。



## 调度的算法

### 先来先服务 (FCFS)

规则：按照作业/进程到达的先后顺序进行服务。

![fcfs](/Users/yzhbradoodrrpurp/Desktop/OS/notes/resources/fcfs.png)

### 短作业优先 (SJF)

规则：服务时间最少的作业/进程最先被服务。

#### 非抢占式

![sjf](/Users/yzhbradoodrrpurp/Desktop/OS/notes/resources/sjf.png)

#### 抢占式

![sjf_](/Users/yzhbradoodrrpurp/Desktop/OS/notes/resources/sjf_.png)

### 高响应比优先 (HRRN)

规则：在每次调度时先计算各个作业/进程的响应比，选择响应比最高的作业/进程为其服务。

![hrrn](/Users/yzhbradoodrrpurp/Desktop/OS/notes/resources/hrrn.png)

### 时间片轮转 (RR)

规则：按照各进程到达的先后顺序，轮流让各个进程执行一个时间片 (比如 100ms)，若进程未在一个时间片内执行完毕，则剥夺处理机，将 进程重新放进就绪队列末尾排队。

![rr2](/Users/yzhbradoodrrpurp/Desktop/OS/notes/resources/rr2.png)

![rr5](/Users/yzhbradoodrrpurp/Desktop/OS/notes/resources/rr5.png)

### 优先级调度算法 (PSA)

规则：每个作业/进程有各自的优先级，调度时选择优先级最高的作业/进程。

#### 非抢占式

![psa](/Users/yzhbradoodrrpurp/Desktop/OS/notes/resources/psa.png)

#### 抢占式

![psa_](/Users/yzhbradoodrrpurp/Desktop/OS/notes/resources/psa_.png)

### 多队列调度算法

![multiqueue](/Users/yzhbradoodrrpurp/Desktop/OS/notes/resources/multiqueue.jpg)

<iframe src="//player.bilibili.com/player.html?isOutside=true&aid=70156862&bvid=BV1YE411D7nH&cid=1102532906&p=25" scrolling="no" border="0" frameborder="no" framespacing="0" allowfullscreen="true"></iframe>