# 进程通信

## 基本概念

进程通信 (Inter-Process Communication) 就是两个进程之间产生数据交互。

## 通信方式

### 共享存储器系统

为了传输大量数据，在存储器中划出了一块共享存储区，诸进程可通过对共享存储区中数据的读或写来实现通信。

![shm](/Users/yzhbradoodrrpurp/Desktop/OS/notes/resources/shm.png)

### 管道通信系统

利用一个缓冲传输介质（内存或文件）连接两个相互通信的进程；字符流方式写入读出，先进先出顺序。

管道通信机制必须提供的协调能力：

- 互斥：即当一个进程正在对 pipe 执行读/写操作时，其它(另一)进程必须等待
- 同步：指当输入进程把一定数量 (如 4 KB) 的数据写入 pipe，便去睡眠等待，直到读输出进程取走数据后，再把它唤醒；当读进程读一空 pipe 时，也应睡眠等待，直至写进程将数据写入管道后才将之唤醒
- 确定对方是否存在：只有确定了对方已存在时，才能进行通信

![pipeline](/Users/yzhbradoodrrpurp/Desktop/OS/notes/resources/pipeline.png)

### 消息传递系统

进程间的数据交换以**格式化的消息** (Message) 为单位。进程通过操作系统提供的“发送消息/接收消息”两个原语进行数据交换。

![message](/Users/yzhbradoodrrpurp/Desktop/OS/notes/resources/message.png)

消息传递系统又分为两种方式：

- 直接消息传递系统
- 间接消息传递系统

#### 直接消息传递系统

发送进程利用OS所提供的发送命令，直接把消息发送给目标进程。

<div style="position: relative; width: 100%; max-width: 960px;">
  <iframe
    src="https://player.bilibili.com/player.html?isOutside=true&aid=70156862&bvid=BV1YE411D7nH&cid=1103498484&p=15"
    style="position: absolute; inset: 0; width: 100%; height: 100%; border: 0;"
    scrolling="no"
    allowfullscreen="true">
  </iframe>
</div>

#### 间接消息传递系统

间接通信方式指进程之间的通信需要通过作为共享数据结构的实体。该实体用来暂存发送进程发送给目标进程的消息；接收进程则从该实体中取出对方发送给自己的消息。通常把这种中间实体称为信箱。

<div style="position: relative; width: 100%; max-width: 960px;">
  <iframe
    src="https://player.bilibili.com/player.html?isOutside=true&aid=70156862&bvid=BV1YE411D7nH&cid=1103498484&p=15"
    style="position: absolute; inset: 0; width: 100%; height: 100%; border: 0;"
    scrolling="no"
    allowfullscreen="true">
  </iframe>
</div>




### 客户机-服务器系统
